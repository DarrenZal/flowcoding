# Methods (Flow Coding)

This directory is Flow Coding's methodology-host layer.

Future method-protocol revisions land here per FC-ADR-0002. The directory is intentionally empty at launch: v1 method documents still live in Spore under the attrition-based migration convention and move here only when superseded by a v-next revision.
